# KSP Mod Localization Translator — Claude Code 개발 프롬프트

## 프로젝트 개요

Kerbal Space Program (KSP 1.12.x) 모드들의 영어 로컬라이제이션 텍스트를 한국어로 번역하는 Python CLI 툴을 개발한다.
Google Gemini API를 사용하여 병렬 번역을 수행하고, 결과물을 ModuleManager 패치 파일(.cfg)로 출력한다.

## 기술 스택

- **Python** (UV 패키지 매니저 기반 프로젝트)
- **Google Gemini API** (번역 엔진, `google-genai` SDK 사용)
- **asyncio + semaphore** 기반 병렬 처리
- **CLI**: `click` 또는 `typer`

## 배경 지식: KSP 로컬라이제이션 시스템

### 구조

KSP 모드는 `.cfg` 파일에 `Localization` 노드로 번역 텍스트를 정의한다:

```cfg
Localization
{
    en-us
    {
        #LOC_ModName_something_title = Some Title
        #LOC_ModName_something_description = A long description of something.
    }
}
```

- 키 형식: `#LOC_` 또는 `#autoLOC_` 접두사 + 고유 식별자
- 값: 영어 텍스트. 아래 특수 요소를 포함할 수 있음
  - `<<1>>`, `<<2>>` 등의 파라미터 플레이스홀더 (절대 번역하면 안 됨)
  - `\n` (줄바꿈)
  - `^N` (천체 이름 포맷팅 태그, 값 끝에 붙음 — 번역하면 안 됨)
  - `<color=...>`, `<b>`, `</b>` 등 Unity Rich Text 태그
  - HTML-like 포맷팅

### 파일 위치 패턴

KSP 모드의 로컬라이제이션 파일은 보통 아래 위치에 존재한다:

1. `GameData/<ModName>/Localization/en-us.cfg` — 가장 일반적
2. `GameData/<ModName>/OtherFeatures/Localization/en-us.cfg` — Kcalbeloh 등 일부 플래닛 팩
3. `GameData/Squad/Localization/dictionary.cfg` — 스톡 게임 (단일 파일, en-us 블록만 포함)

파일 이름은 `en-us.cfg`가 가장 흔하지만, `dictionary.cfg` 같은 다른 이름일 수도 있다.
핵심 판별 기준: 파일 내부에 `Localization { en-us { ... } }` 구조가 있는지 여부.

### 한국어 지원 문제

KSP는 공식적으로 한국어(ko)를 지원하지 않는다. 따라서:
- `ko.cfg`를 만들어도 게임이 로드하지 않음
- 대신 **ModuleManager 패치**로 `en-us` 값을 직접 한국어로 덮어쓰는 방식을 사용

### 출력물: ModuleManager 패치

번역 결과물은 ModuleManager 패치 형식의 `.cfg` 파일이다:

```cfg
// KSP Korean Translation Patch - Generated by ksp-translator
// Source: KcalbelohSystem/OtherFeatures/Localization/en-us.cfg
@Localization:FINAL
{
    @en-us
    {
        @#LOC_KS_Kcalbeloh_description = Kcalbeloh는 초거대질량 블랙홀이며, Kcalbeloh 항성계의 중심입니다.
        @#LOC_KS_Biome_Low = 저지대
        @#LOC_KS_Biome_High = 고지대
    }
}
```

- `@` 접두사는 ModuleManager의 "edit" 연산자 (기존 값을 교체)
- `:FINAL` 패스는 다른 모든 ModuleManager 패치가 적용된 후 실행됨
- 패치 파일은 `GameData/` 아래 아무 위치에나 놓으면 ModuleManager가 자동 인식

## 핵심 요구사항

### 1. 스캐너: 번역 대상 탐색

GameData 폴더를 재귀적으로 스캔하여 로컬라이제이션 파일을 찾는다.

**탐색 기준:**
- `.cfg` 파일 중 `Localization` 노드와 `en-us` 블록을 포함하는 파일
- 단순 텍스트 파싱으로 판별 (정규식으로 `Localization\s*\{` 와 `en-us\s*\{` 패턴 확인)

**제외 대상:**
- `PluginData/`, `cache/`, `Cache/` 디렉토리
- 이미 번역 패치가 생성된 파일 (상태 관리)

### 2. 파서: LOC 키-값 추출

KSP cfg 파일은 표준 포맷이 아니므로 커스텀 파서가 필요하다.

**파싱 규칙:**
- `#LOC_` 또는 `#autoLOC_`로 시작하는 라인에서 `키 = 값` 추출
- 값은 `=` 이후 첫 번째 비공백 문자부터 라인 끝까지
- 여러 줄에 걸친 값은 없음 (KSP LOC은 항상 한 줄)
- `//`로 시작하는 주석 라인은 무시

**추출 예시:**
```
Input:  "        #LOC_KS_Biome_Low = Lowlands"
Output: key="#LOC_KS_Biome_Low", value="Lowlands"

Input:  "        #LOC_KS_name_Kcalbeloh = Kcalbeloh^N"
Output: key="#LOC_KS_name_Kcalbeloh", value="Kcalbeloh^N"
```

### 3. 분류기: 번역 전략 결정

추출된 키-값 쌍을 분류하여 번역 전략을 결정한다.

| 카테고리 | 판별 기준 | 번역 전략 |
|----------|----------|----------|
| 고유명사 (천체 이름 등) | 값이 `^N`으로 끝남 | 번역하지 않음 (스킵) |
| 수치/단위 | 값이 숫자, 단위(m, km, kN 등)만 포함 | 번역하지 않음 |
| 포맷 문자열 | 값이 `<<1>>` 등 플레이스홀더만 포함 | 번역하지 않음 |
| 짧은 UI 텍스트 | 값이 ~5단어 이하 | 배치 번역 (여러 개를 하나의 API 호출로) |
| 긴 설명/리포트 | 값이 ~5단어 초과 | 개별 번역 |

### 4. 번역기: Gemini API 호출

**시스템 프롬프트 (번역 지시):**

```
You are a Korean translator for the game Kerbal Space Program (KSP).
Translate the given English text to natural Korean.

Rules:
1. Preserve all formatting tags exactly: <<1>>, <<2>>, \n, ^N, <color=...>, <b>, </b>, etc.
2. Do NOT translate proper nouns (planet names, star names, kerbal names, part model numbers).
3. Keep the translation concise and natural in Korean gaming context.
4. For science reports and flavor text, maintain the humorous/whimsical tone of KSP.
5. Use 합니다 체 (formal polite) for descriptions, 해요 체 for casual crew reports.
6. Technical terms: use commonly accepted Korean gaming/space terminology.
   Examples: orbit=궤도, thrust=추력, delta-v=델타V, apoapsis=원점, periapsis=근점,
   EVA=선외활동, SAS=자세안정시스템, maneuver node=기동 노드
7. Return ONLY the translated text, no explanations.
```

**배치 번역 (짧은 텍스트):**

여러 키-값 쌍을 JSON 배열로 묶어서 한 번의 API 호출로 처리:

```
Translate each value to Korean. Return a JSON array with the same order.
[
  {"key": "#LOC_KS_Biome_Low", "value": "Lowlands"},
  {"key": "#LOC_KS_Biome_High", "value": "Highlands"},
  {"key": "#LOC_KS_Biome_Coast", "value": "Coast"}
]
```

**병렬 처리:**
- asyncio + semaphore로 동시 요청 수 제한 (기본값: 5)
- 429 (Rate Limit) 응답 시 exponential backoff 재시도
- 실패한 키는 별도 로그에 기록하고 나중에 재시도 가능

### 5. 출력: ModuleManager 패치 생성

각 소스 파일별로 하나의 MM 패치 파일을 생성한다.

**출력 경로 규칙:**
```
소스: GameData/KcalbelohSystem/OtherFeatures/Localization/en-us.cfg
출력: <output_dir>/KcalbelohSystem_ko.cfg

소스: GameData/NearFuturePropulsion/Localization/en-us.cfg
출력: <output_dir>/NearFuturePropulsion_ko.cfg

소스: GameData/Squad/Localization/dictionary.cfg
출력: <output_dir>/Squad_ko.cfg
```

**패치 파일 형식:**
```cfg
// KSP Korean Translation Patch
// Auto-generated by ksp-translator
// Source: KcalbelohSystem/OtherFeatures/Localization/en-us.cfg
// Generated: 2026-03-21
// Keys: 523 translated, 35 skipped

@Localization:FINAL
{
    @en-us
    {
        @#LOC_KS_Kcalbeloh_description = Kcalbeloh는 초거대질량 블랙홀이며...
        @#LOC_KS_Biome_Low = 저지대
        // ... 모든 번역된 키
    }
}
```

### 6. 상태 관리

번역 진행 상태를 JSON 파일로 관리하여 중단/재개를 지원한다.

```json
{
  "version": "1.0",
  "gamedata_path": "C:/Program Files (x86)/Steam/steamapps/common/Kerbal Space Program/GameData",
  "files": {
    "KcalbelohSystem/OtherFeatures/Localization/en-us.cfg": {
      "total_keys": 558,
      "translated": 523,
      "skipped": 35,
      "failed": 0,
      "last_updated": "2026-03-21T14:30:00",
      "hash": "abc123..."
    }
  }
}
```

- 파일 hash로 소스 파일 변경 감지 (변경 시 재번역 트리거)
- 번역 캐시: 이미 번역된 키는 재번역하지 않음

## CLI 인터페이스

```bash
# 전체 GameData 스캔 및 번역 대상 목록 출력
ksp-translator scan --gamedata "C:/path/to/GameData"

# 특정 모드만 번역
ksp-translator translate --gamedata "C:/path/to/GameData" --mod KcalbelohSystem

# 전체 번역
ksp-translator translate --gamedata "C:/path/to/GameData" --output ./patches

# 번역 진행 상태 확인
ksp-translator status

# 실패한 키만 재번역
ksp-translator retry

# 번역 캐시 초기화
ksp-translator clean
```

**환경 변수:**
- `GEMINI_API_KEY`: Gemini API 키 (필수)
- `KSP_GAMEDATA`: GameData 경로 (--gamedata 대신 사용 가능)

## 프로젝트 구조

```
ksp-translator/
├── pyproject.toml          # UV 프로젝트 설정
├── src/
│   └── ksp_translator/
│       ├── __init__.py
│       ├── cli.py           # CLI 엔트리포인트
│       ├── scanner.py       # GameData 스캐너
│       ├── parser.py        # KSP cfg 파서
│       ├── classifier.py    # 키 분류기
│       ├── translator.py    # Gemini API 번역기 (async)
│       ├── generator.py     # MM 패치 생성기
│       ├── state.py         # 상태/캐시 관리
│       └── config.py        # 설정값
├── tests/
│   ├── test_parser.py
│   ├── test_classifier.py
│   └── fixtures/            # 테스트용 샘플 cfg 파일
└── README.md
```

## 주의사항

1. **절대 번역하면 안 되는 것들:**
   - `^N`으로 끝나는 천체 이름 값
   - `<<1>>`, `<<2>>` 등의 파라미터 플레이스홀더
   - Unity Rich Text 태그 (`<color=...>`, `<b>` 등)
   - `\n` (줄바꿈 이스케이프)
   - 키 이름 자체 (`#LOC_...` 부분)
   - ModuleManager 문법 (`@`, `:FINAL` 등)

2. **KSP cfg 파싱의 특이점:**
   - 표준 포맷(JSON, YAML 등)이 아닌 KSP 자체 포맷
   - 중괄호 `{}` 기반 중첩 구조
   - `=` 구분자
   - `//` 주석
   - 탭/스페이스 혼용 들여쓰기
   - UTF-8 인코딩 (BOM 있을 수 있음)

3. **API 비용 관리:**
   - 전체 GameData에 LOC 키가 수천~수만 개 존재할 수 있음
   - 배치 처리로 API 호출 수 최소화
   - 캐싱으로 중복 번역 방지
   - `--dry-run` 옵션으로 실제 API 호출 없이 예상 비용/키 수 확인
   - 혹은 `--pseudo` 옵션으로 실제 API 호출 없이 단순히 prefix만 추가하는 방식으로 의사번역 수행

## 실제 데이터 예시

### Kcalbeloh en-us.cfg 구조 (발췌)

```cfg
Localization
{
    en-us
    {
        // Names (^N 태그 → 번역 스킵)
        #LOC_KS_name_Kcalbeloh = Kcalbeloh^N
        #LOC_KS_name_Anehta = Anehta^N

        // Descriptions (번역 대상)
        #LOC_KS_Kcalbeloh_description = Kcalbeloh is a supermassive black hole, and is the center of Kcalbeloh system. It doesn't have a jet or a superheated blue accretion disk, indicating it has probably not devoured a star in millions of years.

        // Biomes (짧은 UI 텍스트 → 배치 번역)
        #LOC_KS_Biome_Low = Lowlands
        #LOC_KS_Biome_Mid = Midlands
        #LOC_KS_Biome_High = Highlands

        // Science reports (긴 텍스트 → 개별 번역)
        #LOC_KS_SciDef_crewReport_Kcalbeloh_InSpaceLow1 = The crew gazes out...
    }
}
```

### Squad dictionary.cfg 구조 (발췌)

```cfg
Localization
{
    en-us
    {
        #autoLOC_18284 = Stability Assist
        #autoLOC_18285 = Prograde/Retrograde Hold
        #autoLOC_20803 = Jebediah Kerman
        #autoLOC_21131 = <color=<<1>>>[Auto Crew Assignment]: <<2>> just became an intrepid astronaut. (Recruiting Cost: <<3>>)</color>
    }
}
```

### Near Future Propulsion en-us.cfg 구조 (발췌)

```cfg
Localization
{
    en-us
    {
        #LOC_NFPropulsion_Subcategory_plasmaEngines = Plasma Engines
        #LOC_NFPropulsion_manufacturer_kerbkastria_title = Kerb Kastria Inc.
        #LOC_NFPropulsion_switcher_engine_variant1 = 0.625m
    }
}
```

참고:
게임 폴더: C:\Program Files (x86)\Steam\steamapps\common\Kerbal Space Program